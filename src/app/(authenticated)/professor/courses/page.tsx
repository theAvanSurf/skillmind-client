import CoursesList from "@/features/professor/components/CoursesList"

export const metadata = { title: "My Courses — SkillMind" }

export default function ProfessorCoursesPage() {
  return <CoursesList />
}
