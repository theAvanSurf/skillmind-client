import LiveStreamingPage from "@/features/professor/components/LiveStreamingPage"

export const metadata = { title: "Live Streaming — SkillMind" }

export default function ProfessorStreamingPage() {
  return <LiveStreamingPage />
}
